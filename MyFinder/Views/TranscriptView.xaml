<UserControl x:Class="MyFinder.Views.TranscriptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="#1E1E1E">
    <UserControl.Resources>
         <Style TargetType="Button">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                             <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header / Back -->
            <RowDefinition Height="Auto"/> <!-- Video Player -->
            <RowDefinition Height="*"/>    <!-- Transcript -->
        </Grid.RowDefinitions>

        <!-- Header & Controls -->
        <StackPanel Grid.Row="0" Margin="10">
            <DockPanel LastChildFill="True">
                <Button Name="BtnBack" Content="← Back" DockPanel.Dock="Left" Click="BtnBack_Click"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <CheckBox Name="ChkCompare" Content="Compare" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"
                              Checked="ChkCompare_Checked" Unchecked="ChkCompare_Unchecked"/>
                    <Button Name="BtnRetranscribe" Content="⟳ New Transcript" Click="BtnRetranscribe_Click"/>
                </StackPanel>
                <TextBlock Name="TxtFileName" Text="Filename.mp4" Foreground="White" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"/>
            </DockPanel>
            
            <!-- Version Selectors -->
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Name="CboPrimary" SelectionChanged="CboPrimary_SelectionChanged" HorizontalAlignment="Stretch" Height="30" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                 <StackPanel Orientation="Horizontal">
                                     <TextBlock Text="{Binding Model}" FontWeight="Bold" Margin="0,0,5,0"/>
                                     <TextBlock Text="{Binding Created, StringFormat='{}{0:g}'}" Foreground="Gray"/>
                                 </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Name="BtnDelete" Grid.Column="1" Content="🗑" Width="30" Margin="5,0,0,0" Click="BtnDelete_Click" ToolTip="Delete this transcript version" Background="#442222"/>
                </Grid>
                
                <ComboBox Name="CboSecondary" Grid.Column="2" SelectionChanged="CboSecondary_SelectionChanged" 
                          HorizontalAlignment="Stretch" Height="30" VerticalContentAlignment="Center" Visibility="Hidden">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                             <StackPanel Orientation="Horizontal">
                                 <TextBlock Text="{Binding Model}" FontWeight="Bold" Margin="0,0,5,0"/>
                                 <TextBlock Text="{Binding Created, StringFormat='{}{0:g}'}" Foreground="Gray"/>
                             </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </StackPanel>

        <!-- Video Player Section -->
        <Border Grid.Row="1" Background="Black" Height="300" Margin="10,0,10,10" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <MediaElement Name="MediaPlayer" LoadedBehavior="Manual" ScrubbingEnabled="True" MediaOpened="MediaPlayer_MediaOpened" MediaEnded="MediaPlayer_MediaEnded"/>
                
                <!-- Controls Overlay -->
                <Grid Grid.Row="1" Background="#CC000000" VerticalAlignment="Bottom">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Name="BtnPlayPause" Content="▶" Width="40" Margin="5" Click="BtnPlayPause_Click"/>
                    
                    <Grid Grid.Column="1" Margin="10,0">
                        <Slider Name="SliderTime" VerticalAlignment="Center" IsMoveToPointEnabled="True" 
                                PreviewMouseLeftButtonDown="SliderTime_PreviewMouseLeftButtonDown" 
                                PreviewMouseLeftButtonUp="SliderTime_PreviewMouseLeftButtonUp" 
                                ValueChanged="SliderTime_ValueChanged"/>
                    </Grid>
                    
                    <TextBlock Name="TxtTime" Text="00:00 / 00:00" Foreground="White" Grid.Column="2" VerticalAlignment="Center" Margin="5,0,10,0"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Transcript List -->
        <Grid Grid.Row="2" Margin="10,0,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Name="ColSecondary" Width="0"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="TRANSCRIPT (Click to Seek)" Foreground="#AAA" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Grid.Column="2" Text="COMPARISON" Foreground="#AAA" FontWeight="Bold" Margin="0,0,0,5" Visibility="{Binding ElementName=CboSecondary, Path=Visibility}"/>
            
            <ListView Name="LstTranscript" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="White" 
                      SelectionChanged="LstTranscript_SelectionChanged" HorizontalContentAlignment="Stretch">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,1" Padding="5">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#33007ACC"/>
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Start, StringFormat=mm\\:ss}" Foreground="#00CCFF" FontWeight="Bold" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Text}" Foreground="#DDD" TextWrapping="Wrap" Margin="10,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <!-- Secondary List -->
            <ListView Name="LstTranscriptSecondary" Grid.Row="1" Grid.Column="2" Background="Transparent" BorderThickness="0" Foreground="White" 
                      SelectionChanged="LstTranscriptSecondary_SelectionChanged" HorizontalContentAlignment="Stretch" Visibility="Hidden">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,1" Padding="5">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Start, StringFormat=mm\\:ss}" Foreground="#00CCFF" FontWeight="Bold" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Text}" Foreground="#DDD" TextWrapping="Wrap" Margin="10,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <!-- Loading Overlay -->
             <Grid Name="LoadingOverlay" Grid.Row="1" Background="#AA000000" Visibility="Collapsed">
                 <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                     <TextBlock Text="TRANSCRIBING..." Foreground="White" FontSize="18" FontWeight="Bold"/>
                     <TextBlock Text="This may take a while." Foreground="#AAA" HorizontalAlignment="Center"/>
                 </StackPanel>
             </Grid>
        </Grid>
    </Grid>
</UserControl>
