<UserControl x:Class="MyFinder.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800" Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Name="BtnBack" DockPanel.Dock="Left" Content="⬅ Back" Background="Transparent" Foreground="#AAA" BorderThickness="0" Click="BtnBack_Click" Margin="0,0,20,0"/>
            <TextBlock Text="SETTINGS" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Use ScrollViewer for settings content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">
                
                <!-- Scan Settings -->
                <GroupBox Header="Scan Settings" Foreground="#AAA" BorderBrush="#333" Padding="10">
                    <StackPanel>
                        <CheckBox Name="ChkSkipSystem" Content="Skip System Folders (e.g. Windows, Program Files)" Foreground="White" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>

                <!-- General Settings -->
                <GroupBox Header="General" Foreground="#AAA" BorderBrush="#333" Padding="10" Margin="0,10,0,0">
                    <StackPanel>
                        <DockPanel Margin="0,5">
                            <TextBlock Text="Recent Files Duration (Hours):" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Name="TxtRecency" Width="60" HorizontalAlignment="Right" TextAlignment="Center" Background="#2D2D30" Foreground="White" BorderThickness="1" BorderBrush="#444"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>

                <!-- Object Detection -->
                <GroupBox Header="Object Detection" Foreground="#AAA" BorderBrush="#333" Padding="10" Margin="0,10,0,0">
                    <StackPanel>
                        <CheckBox Name="ChkPerson" Content="Detect Persons" Foreground="White" Margin="0,5"/>
                        <CheckBox Name="ChkVehicle" Content="Detect Vehicles (Cars, Trucks, Bikes)" Foreground="White" Margin="0,5"/>
                        <CheckBox Name="ChkAnimal" Content="Detect Animals (Cats, Dogs, Wildlife)" Foreground="White" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>

                <!-- Transcription Settings -->
                <GroupBox Header="Transcription" Foreground="#AAA" BorderBrush="#333" Padding="10" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="Whisper Model (Restart Required):" Foreground="White" Margin="0,0,0,5"/>
                        <ComboBox Name="CboWhisperModel" HorizontalAlignment="Left" Width="200" SelectedIndex="1">
                            <ComboBoxItem Content="Tiny (Fastest, Low Accuracy)"/>
                            <ComboBoxItem Content="Base (Balanced)"/>
                            <ComboBoxItem Content="Small (Good Accuracy)"/>
                            <ComboBoxItem Content="Medium (Better Accuracy, Slow)"/>
                            <ComboBoxItem Content="Large (Best Accuracy, Very Slow)"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <!-- Diagnostics -->
                <GroupBox Header="Diagnostics" Foreground="#AAA" BorderBrush="#333" Padding="10" Margin="0,10,0,0">
                    <StackPanel>
                        <WrapPanel>
                            <Button Name="BtnDebugTime" Content="Debug Timestamp" Background="#333" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,5,5" Click="BtnDebugTime_Click"/>
                            <Button Name="BtnDebugFace" Content="Debug Face" Background="#333" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,5,5" Click="BtnDebugFace_Click"/>
                            <Button Name="BtnDebugVoice" Content="Debug Voice" Background="#333" Foreground="White" BorderThickness="0" Padding="10,5" Margin="0,0,5,5" Click="BtnDebugVoice_Click"/>
                        </WrapPanel>
                        <TextBox Name="TxtDebugOutput" Height="100" Margin="0,10,0,0" Background="#111" Foreground="#00FF00" FontFamily="Consolas" FontSize="11" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Visibility="Collapsed" TextWrapping="Wrap"/>
                    </StackPanel>
                </GroupBox>

                <!-- Dangerous Actions -->
                <GroupBox Header="Actions" Foreground="#AAA" BorderBrush="#333" Padding="10" Margin="0,10,0,0">
                    <Button Name="BtnWipe" Content="⚠ Wipe Database &amp; Reset" Background="#aa3333" Foreground="White" BorderThickness="0" Padding="10,5" Click="BtnWipe_Click"/>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
             <Button Name="BtnSave" Content="Save Settings" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="20,10" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
